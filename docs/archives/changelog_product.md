# Журнал продуктовых решений

> **Архивный документ.** Агенты не используют его как источник контекста: работа ведётся только по актуальной документации — [`../project_description.md`](../project_description.md), [`../technical_description.md`](../technical_description.md), [`../code_style.md`](../code_style.md). Читать этот файл разрешено в двух случаях: чтобы добавить новую запись или если разработчик прямо попросил изучить историю решений. Во всех остальных случаях включать его содержимое в запрос запрещено.
>
> Здесь фиксируются решения о поведении продукта: режимы работы, значения по умолчанию, ограничения и инварианты. Технические решения — в [`changelog_technical.md`](changelog_technical.md).
>
> **Правила ведения**
>
> - Новая запись добавляется **сверху** таблицы: порядок обратный хронологическому, самое свежее решение первое.
> - Запись вносится в том же изменении, в котором принято решение. Колонки: дата, решение, причина.
> - Существующие записи не редактируются и не удаляются. Отменённое решение оформляется новой записью сверху — с причиной отмены и указанием даты отменяемой записи.
> - Изменённое или отменённое решение, требование или раздел **не остаётся** в актуальном документе ни в каком виде — в том числе с пометкой «устарело» или «отменено». Прежний текст удаляется из актуального документа и переносится сюда: в таблицу добавляется запись с датой переноса и причиной, объёмный текст выносится отдельным файлом (см. ниже). В актуальных документах всегда только действующее состояние; ссылок на конкретные архивные записи в них не оставляют — достаточно общей ссылки на журнал в шапке.
> - Объёмный перенесённый раздел выносится отдельным файлом `archive_<тема>_<ГГГГ-ММ-ДД>.md` в этом каталоге, а в таблице остаётся строка со ссылкой на него.
> - Отменённое или изменённое требование (FR) целиком удаляется из `project_description.md`, его прежний текст переносится сюда, а в таблицу добавляется запись с причиной. Номер FR не переиспользуется: он навсегда закреплён за перенесённым требованием.

| Дата | Решение | Причина |
|---|---|---|
| 2026-09-12 | Проект и команда переименованы из `local-llm-runner` в `locallm`; открытый вопрос о коротком имени команды закрыт | Новое имя короче и стало каноническим для пользовательского запуска и документации. |
| 2026-09-12 | Жёсткое значение модели по умолчанию (`ornith-1.5-35b-a3b`) отменено: модель выбирается по приоритету флаг > env > настройки > первая с `tool_use`, предпочитая загруженную | Значение по умолчанию было привязано к машине автора и ломалось, как только модель не скачана; агентская работа невозможна без `tool_use`, поэтому выбор должен опираться на возможности модели. Отменяет решение от 2026-09-11 о фиксированной модели по умолчанию. |
| 2026-09-12 | Windows — целевая платформа наравне с macOS и Linux; различается только уровень песочницы | Инструмент должен работать не только на машине автора. Отменяет решение от 2026-09-11 «Windows — не целевая платформа». |
| 2026-09-12 | Метрики дополнительно пишутся отдельным файлом на сессию в каталог метрик | Измеримость — свойство продукта: сравнение моделей требует сохранённых, а не однократно напечатанных результатов. |
| 2026-09-12 | Одновременно в памяти держится одна модель: перед загрузкой новой все загруженные выгружаются | Ресурсов на две модели нет; частичная выгрузка приводила к отказам загрузки по памяти. |
| 2026-09-12 | Изменяющие действия требуют подтверждения; для неинтерактивных прогонов нужен явный `--yes` | Вывод модели — недоверенный ввод; молчаливое изменение файлов неприемлемо. |
| 2026-09-12 | Введены потолки расходов (ходы, токены, время) и отдельный код выхода 3 при упоре в них | Контролируемость: вызывающая сторона должна отличать «не смог» от «не дали закончить». |
| 2026-09-12 | Ретенция истории сессий: 50 сессий, 500 МБ, 90 дней — что раньше; каждое ограничение обнуляется по отдельности, все три сразу — нет; самая свежая сессия не удаляется | Замер: сессия — 120 КБ–1 МБ, поэтому без ограничений каталог растёт неограниченно; полностью отключить историю можно явным флагом или настройкой. |
| 2026-09-12 | Уровень песочницы и политика (`required`, `best-effort`, `off`) видны пользователю и попадают в файл метрик | Изоляция различается по платформам, и пользователь должен знать, что именно было применено, а не предполагать. |
| 2026-09-12 | Subagents не реализуются | Одна модель в памяти делает параллелизм фикцией, переключение контекстов срывает prompt-кэш, цепочки без присмотра множат ошибки tool calling. |
| 2026-09-12 | Методология разработки (как писать планы, как разбивать задачи, какие роли) — вне рамок продукта | Проект предоставляет механизм; методику определяет пользователь в своих файлах. |
| 2026-09-12 | Открытые вопросы о терминальном интерфейсе, файле конфигурации, сохранении сессий и чтении `AGENTS.md` закрыты решениями: построчный интерфейс сейчас и полноэкранный позже, JSON-настройки в каталоге пользователя, история сессий в JSONL, чтение `AGENTS.md`/`CLAUDE.md` в системный промпт | Вопросы решены, поэтому удалены из раздела открытых вопросов актуального документа. |
| 2026-09-11 | Метрики и диагностика — в stderr, результат — в stdout | Вывод one-shot режима можно перенаправлять без примесей. |
| 2026-09-11 | Рабочая директория = каталог запуска | Прототип работал в каталоге скриптов — это было ограничением. |
| 2026-09-11 | Агент не делает git-коммиты | Унаследовано от прототипа: контроль истории остаётся у пользователя. |
| 2026-09-11 | Сохранена переменная `LOCAL_MODEL` и модель по умолчанию из прототипа | Совместимость с привычным запуском. |
