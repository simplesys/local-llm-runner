# Журнал технических решений

> **Архивный документ.** Агенты не используют его как источник контекста: работа ведётся только по актуальной документации — [`../project_description.md`](../project_description.md), [`../technical_description.md`](../technical_description.md), [`../code_style.md`](../code_style.md). Читать этот файл разрешено в двух случаях: чтобы добавить новую запись или если разработчик прямо попросил изучить историю решений. Во всех остальных случаях включать его содержимое в запрос запрещено.
>
> **Правила ведения**
>
> - Новая запись добавляется **сверху** таблицы: порядок обратный хронологическому, самое свежее решение первое.
> - Запись вносится в том же изменении, в котором принято решение. Колонки: дата, решение, причина.
> - Существующие записи не редактируются и не удаляются. Отменённое решение оформляется новой записью сверху — с причиной отмены и указанием даты отменяемой записи.
> - Изменённое или отменённое решение, раздел или описание **не остаётся** в актуальном документе ни в каком виде — в том числе с пометкой «устарело» или «отменено». Прежний текст удаляется из актуального документа и переносится сюда: в таблицу добавляется запись с датой переноса и причиной, объёмный текст выносится отдельным файлом (см. ниже). В актуальных документах всегда только действующее состояние; ссылок на конкретные архивные записи в них не оставляют — достаточно общей ссылки на журнал в шапке.
> - Объёмный перенесённый раздел выносится отдельным файлом `archive_<тема>_<ГГГГ-ММ-ДД>.md` в этом каталоге, а в таблице остаётся строка со ссылкой на него.

| Дата | Решение | Причина |
|---|---|---|
| 2026-09-12 | Go-модуль, бинарник, точка входа и каталог состояния переименованы с `local-llm-runner` на `locallm`; import path теперь `github.com/simplesys/locallm`, точка входа — `cmd/locallm`, каталог состояния — `os.UserConfigDir()/locallm` | Технические идентификаторы должны совпадать с новым именем проекта, чтобы сборка, документация и состояние на диске не расходились. |
| 2026-09-12 | `CGO_ENABLED=0` — жёсткое правило сборки; проверяется целью `make crossbuild` | Кросс-компиляция под Linux и Windows одной командой без тулчейнов; исключает зависимости, требующие CGO. |
| 2026-09-12 | Переключение моделей — через CLI `lms`, а не через REST или JIT-загрузку | Проверено: REST-эндпоинтов загрузки и выгрузки у LM Studio нет, а JIT не выгружает вручную загруженные модели и не даёт прогресса. Внутренний WebSocket-API приложения не документирован и не используется. |
| 2026-09-12 | Собственная карта символов кода (regex-сигнатуры, парсеры, tree-sitter) не делается | Замеры: дерево из 3362 файлов ≈56 000 токенов, сигнатуры по regex ≈49 000 токенов — без ранжирования бесполезно; tree-sitter требует CGO, что противоречит правилу сборки. Навигация строится на дереве файлов, `glob`/`grep`/`read_file` и опциональном внешнем индексаторе через MCP. |
| 2026-09-12 | Клиент LLM пишем сами на `net/http` | LM Studio расширяет формат OpenAI (`reasoning_content`), SSE-парсер — около сотни строк, сторонние SDK связывают своими типами и нетривиальной работы не снимают. |
| 2026-09-12 | Занятость контекста оценивается как ≈4 символа на токен с коррекцией по фактическому `usage.prompt_tokens` | Эндпоинта токенизации у LM Studio нет (проверено `/api/v0/tokenize`, `/v1/internal/tokenize`, `/api/v0/models/tokenize`). |
| 2026-09-12 | Песочница на macOS — `sandbox-exec` (Seatbelt), несмотря на формально устаревший API; реализация изолирована в `internal/sandbox` с разделением по build-тегам | Единственный механизм изоляции дочернего процесса без внешних зависимостей; замена не затронет остальной код. Пути в профиле разворачиваются через `EvalSymlinks`, иначе `/var` против `/private/var` ломает разрешения на macOS. |
| 2026-09-12 | Каталог состояния переопределяется переменной `LOCAL_LLM_CONFIG_DIR` | Нужно и пользователю (состояние в другом месте), и тестам: иначе прогон трогает настоящий каталог настроек. |
| 2026-09-12 | SIGINT обрабатывает `app`, а не корневой контекст в `main`; в `main` остаётся только SIGTERM | Первое нажатие Ctrl+C должно прерывать текущую задачу, а не процесс; политика прерывания принадлежит тому коду, который знает о сессии. |
| 2026-09-12 | В интерактивном режиме ввод читает одна горутина-владелец, и она не печатает ничего | Два писателя в stderr дают перемешанный вывод и гонку (обнаружена `-race`); подтверждения читаются из того же канала, а не напрямую из stdin. |
| 2026-09-12 | Файл сессии — append-only JSONL плюс индекс, перестраиваемый сканом; полезная нагрузка хранится как `json.RawMessage` | Обрыв записи теряет только последнюю строку; пакет истории остаётся листовым и не зависит от типов LLM и агента. |
| 2026-09-12 | Новая внешняя зависимость добавляется только вместе с кодом, который её использует | Иначе `go mod tidy` вычищает неиспользуемые модули, а `go.sum` наполняется раньше пользы. |
| 2026-09-12 | Изменённые и отменённые требования, решения и разделы переносятся в архив целиком; пометки «устарело» и «отменено» в актуальных документах запрещены | Актуальные документы читаются как срез текущего состояния, без разбора, что из написанного ещё действует. |
| 2026-09-12 | Записи в журналах — в обратном хронологическом порядке; архив не используется агентами без явной просьбы разработчика | Свежие решения видны сразу; архив не раздувает контекст запроса. |
| 2026-09-12 | Журналы решений вынесены в `docs/archives/` | Описания проекта остаются коротким срезом текущего состояния, а история решений не растёт внутри них. |
| 2026-09-12 | Скиллы хранятся в `.agents/skills/`, `.claude/skills` — симлинк на этот каталог | Скиллы не привязаны к конкретному клиенту, как и `AGENTS.md` с симлинком `CLAUDE.md`. |
| 2026-09-12 | Сообщения коммитов — Conventional Commits на английском; формируются скиллом `commit-message` | Единый предсказуемый формат истории; текст готовится по фактическому diff. |
| 2026-09-11 | golangci-lint v2 с gofumpt и goimports | Единый инструмент для линтинга и форматирования. |
| 2026-09-11 | Файловый доступ инструментов — только через `os.Root` | Надёжная защита от выхода за рабочую директорию вместо ручной проверки путей. |
| 2026-09-11 | Процессное окружение передаётся в `app.Run` через `Options` | Тестируемость без глобального состояния. |
| 2026-09-11 | Флаги — стандартный `flag`, без cobra и аналогов | Флагов мало, подкоманд нет; внешняя зависимость не нужна. |
| 2026-09-11 | Собственный агентский цикл поверх OpenAI-совместимого API вместо обёрток над aider/Codex | Точные метрики из `usage`, контроль инструментов и безопасности. |
| 2026-09-11 | Go, один бинарник, стандартная библиотека в первую очередь | Убрать зависимость прототипа от Python, `uv`, Homebrew и сторонних агентов. |
